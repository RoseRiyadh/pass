#include "pch.h"
#include <string>
#include <iostream>
using namespace std;
int main()
{
	const int p1 = 279834;
	const int p2 = 388930;
	const int p3 = 118920;
	string a, b, c, d;
	int s, i=1;
	a = "ceo_uot";
	b = "dev_uot";
	c = "man_uot";
	cout << "Welcome to your profile , enter your username to continue:" << endl;
	cin >> d;
	if (d == a)
	{
		cout << "enter your password:" << endl;
		cin >> s;	cout << endl;
		while (s != p1 && i <= 3)
		{
			cout << "please try again:";
			cin >> s;
			i++;
		}
		if (s == p1)
			cout << "Welcome.";
		else
			cout << "Wrong password, try again later.";
		return 0;

	}
	else if (d == b)
	{
		cout << "enter your password:" << endl;
		cin >> s;	cout << endl;
		while (s != p2 && i <= 3)
		{
			cout << "please try again:";
			cin >> s;
			i++;
		}
		if (s == p2)
			cout << "Welcome.";
		else
			cout << "Wrong password, try again later.";
		return 0;
	}
	else if (d == c)
	{
		cout << "enter your password:" << endl;
		cin >> s;	cout << endl;
		while (s != p3 && i <= 3)
		{
			cout << "please try again:";
			cin >> s;
			i++;
		}
		if (s == p3)
			cout << "Welcome.";
		else
			cout << "Wrong password, try again later.";
		return 0;
	}
	else
		cout << "undefined username , try again";


	return 0;
}
